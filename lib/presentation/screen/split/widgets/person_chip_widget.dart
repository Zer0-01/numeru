import 'package:flutter/material.dart';

class PersonChipWidget extends StatelessWidget {
  const PersonChipWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
